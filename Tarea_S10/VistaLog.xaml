<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tarea_S10.VistaLog"
             BackgroundColor="#24252a"
             Title="Historial">
    

    <StackLayout>
        <Button x:Name="btnReset" BackgroundColor="#2B2C31" TextColor="White" WidthRequest="50" HeightRequest="50" FontSize="Title" Text="Delete content"></Button>
    
    <ListView x:Name="coleccion">
        
        <ListView.ItemTemplate>

            <DataTemplate>
                <TextCell Text="{Binding}" TextColor="White"/>
                
            </DataTemplate>
        </ListView.ItemTemplate>
        
        
    </ListView>

        <!--<ListView x:Name="coleccion" >
        
    </ListView>-->
    </StackLayout>
</ContentPage>